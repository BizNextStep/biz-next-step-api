{
  "name": "biz-next-step-api",
  "version": "1.0.0",
  "description": "API to help SMEs enhance leadership and automate business processes.",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",  // Start server in production mode
    "dev": "nodemon src/app.js",  // Start server in development mode
    "test": "echo \"Error: no test specified\" && exit 1"  // Placeholder for test script
  },
  "dependencies": {
    "cors": "^2.8.5",  // Middleware to enable CORS
    "dotenv": "^16.0.3",  // Loads environment variables from .env file
    "express": "^4.18.2",  // Web framework for Node.js
    "jsonwebtoken": "^8.5.1",  // JSON Web Token library for authentication
    "pg": "^8.8.0"  // PostgreSQL client for Node.js
  },
  "devDependencies": {
    "nodemon": "^2.0.15"  // Automatically restarts the server on changes in development
  },
  "engines": {
    "node": ">=14.x",  // Specifies the Node.js version required
    "npm": ">=6.x"  // Specifies the npm version required
  },
  "author": "Your Name",  // Replace with your actual name
  "license": "MIT"
}
